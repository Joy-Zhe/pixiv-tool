<Page x:Class="PixivTool.PidPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="PidPage"
      MinHeight="600"
      MinWidth="720"
      Height="600"
      Width="720">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60" />
            <ColumnDefinition Width="50" />
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="80" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="120" />
            <RowDefinition Height="40" />
            <RowDefinition Height="5" />
            <RowDefinition Height="40" />
            <RowDefinition Height="250" />
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Label VerticalContentAlignment="Center" Grid.Column="1" Grid.Row="1" Content="PID" />

        <TextBox VerticalContentAlignment="Center"
                 Grid.Column="2"
                 Grid.Row="1"
                 Text="{Binding Pid, UpdateSourceTrigger=PropertyChanged}" />

        <Button Grid.Column="4" Grid.Row="1" Content="Download" Command="{Binding DownloadCommand}" />
        <Button Grid.Column="4" Grid.Row="3" Content="Preview" Command="{Binding PreviewCommand}" />

        <Label Grid.Column="5"
               Grid.Row="5"
               Content="Preview"
               VerticalContentAlignment="Center"
               HorizontalAlignment="Center"
               FontSize="20"/>
        <Image Grid.Column="5"
               Grid.Row="4"
               Width="250"
               Height="250"
               Source="{Binding PreviewImage}" />
    </Grid>
</Page>
