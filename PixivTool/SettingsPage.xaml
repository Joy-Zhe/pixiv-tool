<Page x:Class="PixivTool.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="SettingsPage"
      MinHeight="600"
      MinWidth="720"
      Height="600"
      Width="720">
    <Grid Margin="8">
        <Border Style="{StaticResource CardStyle}" MaxWidth="760" HorizontalAlignment="Left">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Settings" Style="{StaticResource TitleTextStyle}" />

                <Grid Grid.Row="1" Margin="0,6,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="100" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="Rank Path" FontWeight="SemiBold" />
                    <Border Grid.Column="1"
                            Margin="0,4,0,10"
                            Padding="10,8"
                            Background="#F7FAFD"
                            BorderBrush="#C2CDD8"
                            BorderThickness="1"
                            CornerRadius="6">
                        <TextBlock Text="{Binding RankDownloadPath}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                    </Border>
                    <Button Grid.Column="2" Content="Apply" Command="{Binding ApplyRankPathCommand}" Margin="8,4,0,10" />
                </Grid>

                <Grid Grid.Row="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="130" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="100" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="PID Path" FontWeight="SemiBold" />
                        <Border Grid.Column="1"
                                Margin="0,4,0,10"
                                Padding="10,8"
                                Background="#F7FAFD"
                                BorderBrush="#C2CDD8"
                                BorderThickness="1"
                                CornerRadius="6">
                            <TextBlock Text="{Binding PidDownloadPath}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                        </Border>
                        <Button Grid.Column="2" Content="Apply" Command="{Binding ApplyPidPathCommand}" Margin="8,4,0,10" />
                    </Grid>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="130" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="100" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="Cookie" FontWeight="SemiBold" />
                        <TextBox Grid.Column="1"
                                 Text="{Binding CookieRawMessage, UpdateSourceTrigger=PropertyChanged}" />
                        <Button Grid.Column="2" Content="Save" Command="{Binding SaveCommand}" Margin="8,4,0,10" />
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Page>
