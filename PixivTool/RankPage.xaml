<Page x:Class="PixivTool.RankPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="RankPage"
      MinHeight="600"
      MinWidth="720"
      Height="600"
      Width="720">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <RowDefinition Height="80" />
            <RowDefinition Height="80" />
            <RowDefinition Height="80" />
            <RowDefinition Height="80" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Column="1" Grid.Row="2">
            <Label Content="Content Type" />
            <ComboBox SelectedIndex="{Binding SelectedDownloadOption}">
                <ComboBoxItem Content="All" />
                <ComboBoxItem Content="Illust" />
                <ComboBoxItem Content="Manga" />
            </ComboBox>
        </StackPanel>

        <StackPanel Orientation="Vertical" Grid.Column="2" Grid.Row="2">
            <Label Content="Time Span" />
            <ComboBox SelectedIndex="{Binding SelectedTimeSpan}">
                <ComboBoxItem Content="Daily" />
                <ComboBoxItem Content="Weekly" />
            </ComboBox>
        </StackPanel>

        <StackPanel Orientation="Vertical" Grid.Column="3" Grid.Row="2">
            <Label Content="Exact Date" />
            <ComboBox>
                <ComboBoxItem Content="null" />
                <ComboBoxItem Content="null" />
                <ComboBoxItem Content="null" />
            </ComboBox>
        </StackPanel>

        <StackPanel Orientation="Vertical" Grid.Column="4" Grid.Row="3">
            <Button Content="Download" Command="{Binding DownloadCommand}" />
        </StackPanel>

        <CheckBox Content="R18" IsChecked="{Binding IsR18}" />
    </Grid>
</Page>
